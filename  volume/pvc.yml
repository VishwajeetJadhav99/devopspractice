apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pv-claim
  labels:
    app: pv-claim
spec:
  storageclass: standard
  volumeMode: Filesystem
  acessMode:
    -ReadWriteOnce
  resources:
    requests:
      storage: 10Gi